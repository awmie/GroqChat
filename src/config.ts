export const config = {
    COT_PROMPT: import.meta.env.VITE_COT_PROMPT || "Break the PROBLEM into subproblems, for each subproblem Think Step-by-Step and recheck each step to ensure accuracy and correctness"
};
